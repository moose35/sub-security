console.log('chocolate cake');
// `let` is a keyword 
// `x` is the name of the variable
// `=` is the assignment operator
// `1` is the value
let x = 1;

//camel case
let firstName = 'Sharifa';
let numberOfPets = 2;

// expressions and operators
let y = 7; // expression
y = y + 1; // expression
y +=1; 
y++;

console.log(y);

// strings/concatenation

console.log('1' + 2);

// conditions (if)
if(9){
    console.log('condition was hit');
}

//undefined is an error, null is an actual value we assign

// loops

for (let i = 0; i < 3; i++){
    console.log(i);
}